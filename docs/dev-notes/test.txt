
Can you help me update the `config.json` and `tools/personalize.js` to work with the new placeholder format `your-github-handle` instead of `{{placeholder}}`?

I'd like to add a simple blog section to my portfolio. Can you suggest how to structure the HTML and CSS for it?




<!-- Hire Me Button -->
<button id="hire-me" class="px-4 py-2 border border-[var(--accent)] text-[var(--accent)] hover:bg-[var(--accent)] hover:text-white font-mono text-sm rounded-lg transition-colors" aria-label="Hire me">
HIRE ME
</button>

/* Hire-Me Button */
/* Styles for the "Hire Me" call-to-action button. */
#hire-me {
  position: fixed;
  right: -5px; /* Slightly tucked away */
  top: 50%;
  transform: translateY(-50%);
  
  /* Vertical text */
  writing-mode: vertical-rl;
  text-orientation: upright;
  letter-spacing: 2px;
  
  /* Slide-out styling */
  padding: 1.5rem 0.6rem;

  color: var(--accent); /* Use theme text color for consistency */
  border: 1px solid var(--accent);
  transition: all 0.4s ease-in-out; /* Smoother, more professional transition */
  cursor: pointer;
  z-index: 30; /* Ensure it stays above other content */
}

#hire-me:hover {
  right: 0; /* Slides fully into view */
  background-color: var(--accent);
  color: #fff;
  padding-right: 1.2rem; /* Adds extra width on hover */
  animation: none; /* Disable pulse on hover for better UX */
}

/* The Notification Dot */
#hire-me::after {
  content: "";
  position: absolute;
  top: -5px;
  left: 50%;
  transform: translateX(-50%);
  width: 10px;
  height: 10px;
  background-color: var(--green); /* Use green color for dot */
  border-radius: 50%;
  box-shadow: 0 0 0 rgba(59, 246, 68, 0.7); /* Use green color's rgba for pulse */
  animation: pulse-dot 1.5s infinite;
}

/* Pulse Animation */
@keyframes pulse-dot {
  0% {
    box-shadow: 0 0 0 0 rgba(59, 246, 68, 0.7); /* Use green color's rgba for pulse */
  }
  70% {
    box-shadow: 0 0 0 10px rgba(59, 246, 68, 0); /* Use green color's rgba for pulse */
  }
  100% {
    box-shadow: 0 0 0 0 rgba(59, 246, 68, 0); /* Use green color's rgba for pulse */
  }
}